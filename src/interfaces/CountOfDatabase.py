class CountOfDatabase:
    def count_of_database_method(self):
        count_of_database = self.__len__()
        print(f"Result of the research: {count_of_database}")
        # if count_of_database == 0:
        #     print(f"Nothing or nobody has been found in the database.")
        #     # raise ValueError(f"None author born in {year_of_birth}")
        # elif count_of_database == 1:
        #     print(f"{count_of_database} has been found in the database.")
        # else:
        #     print(f"{count_of_database} have been found in the database.")

